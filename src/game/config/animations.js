export const token = {
	// Token moving duration for each step
	DURATION_MOVE: 300,
	// Delay before moving to next step
	PAUSE_BEFORE_NEXT_STEP: 300,
	// Delay for next sequence after action initiated/finished
	ACTION_DELAY: 500,
	// Transition duration for active indicator
	DURATION_ACTIVE_INDICATOR_TRANSITION: 300,
};
